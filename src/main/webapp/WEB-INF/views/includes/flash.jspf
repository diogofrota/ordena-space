<%@ taglib prefix="c" uri="jakarta.tags.core" %>
<c:if test="${not empty flashMessage}">
    <div class="flash flash-${flashType}">
        <span>${flashMessage}</span>
        <button type="button" class="flash-close" aria-label="Fechar">x</button>
    </div>
</c:if>
